# 🔧 安装和测试说明

## 重新加载插件

由于我们修改了插件代码，需要重新加载插件才能生效：

### 步骤：

1. **打开扩展程序页面**
   - 在 Chrome 地址栏输入：`chrome://extensions/`
   - 或者：右键点击插件图标 → 管理扩展程序

2. **重新加载插件**
   - 找到「DeepL 段落翻译助手」
   - 点击刷新按钮 🔄
   - 或者先移除插件，然后重新「加载已解压的扩展程序」

3. **刷新测试页面**
   - 如果之前打开了测试网页，需要刷新页面（F5 或 Cmd+R）
   - 这样 content script 才能重新加载

## 测试新功能

### 功能 1️⃣：选中文本后点击图标自动填充

**测试步骤：**

1. 打开测试页面 `test.html`（双击打开或拖到浏览器）
2. 在页面上选中一段文本（比如英文段落）
3. **保持文本选中状态**
4. 点击浏览器工具栏的插件图标 🌐
5. 弹窗应该会打开，并且选中的文本已经自动填充到文本框中
6. 点击「翻译」按钮即可查看结果

**预期结果：**
- ✅ 弹窗打开
- ✅ 文本框中有刚才选中的内容
- ✅ 可以直接点击翻译按钮

### 功能 2️⃣：选中文本出现翻译按钮

**测试步骤：**

1. 在测试页面选中一段文本
2. 释放鼠标（mouseup）
3. 应该在光标附近出现 🌐 翻译 按钮
4. 点击按钮查看翻译结果

**预期结果：**
- ✅ 选中文本后出现蓝色翻译按钮
- ✅ 点击按钮显示翻译中...
- ✅ 显示翻译结果的白色浮窗

### 功能 3️⃣：右键菜单翻译

**测试步骤：**

1. 在测试页面选中一段文本
2. 右键点击选中的文本
3. 在菜单中选择「使用 DeepL 翻译」
4. 查看翻译结果

**预期结果：**
- ✅ 右键菜单出现翻译选项
- ✅ 点击后显示翻译结果

## 常见问题排查

### ❌ 问题 1：选中文本点击图标，文本没有自动填充

**可能原因：**
- 插件没有重新加载
- 页面没有刷新
- 浏览器权限问题

**解决方法：**
```bash
1. chrome://extensions/ → 找到插件 → 点击刷新按钮 🔄
2. 刷新测试页面（F5）
3. 重新选中文本 → 点击插件图标
```

### ❌ 问题 2：选中文本后翻译按钮不出现

**可能原因：**
- content.js 没有加载
- 选中文本太长或太短
- 页面脚本冲突

**解决方法：**
```bash
1. 刷新页面（F5）
2. 选中 10-100 字符的文本试试
3. 打开开发者工具（F12）查看是否有错误
```

### ❌ 问题 3：提示 "无法获取选中文本"

**可能原因：**
- manifest.json 权限不足
- 浏览器版本太旧

**解决方法：**
```bash
1. 检查 manifest.json 中是否有 "scripting" 权限
2. 确保 Chrome 版本 >= 88
3. 重新加载插件
```

### ❌ 问题 4：翻译失败，显示 403 错误

**可能原因：**
- API Key 错误或过期
- 超出免费额度
- 网络连接问题

**解决方法：**
```bash
1. 检查 API Key 是否正确（去掉多余空格）
2. 登录 DeepL 控制台查看用量
3. 测试网络是否能访问 api-free.deepl.com
```

## 开发者工具调试

如果功能不正常，可以使用开发者工具调试：

### 1. 调试 popup.js（弹窗脚本）

```bash
1. 点击插件图标打开弹窗
2. 右键点击弹窗 → 检查
3. 在 Console 标签查看错误信息
4. 可以在 Sources 标签设置断点调试
```

### 2. 调试 content.js（页面脚本）

```bash
1. 在测试页面按 F12 打开开发者工具
2. 在 Console 标签查看错误
3. 在 Sources 标签找到 content.js 设置断点
4. 选中文本触发事件，观察执行流程
```

### 3. 调试 background.js（后台脚本）

```bash
1. 访问 chrome://extensions/
2. 找到插件，点击「service worker」链接
3. 在打开的开发者工具中查看 Console
```

## 测试检查清单

在发布前，请确保以下功能都正常：

- [ ] 插件可以成功加载到 Chrome
- [ ] 图标正常显示
- [ ] 打开弹窗，界面显示正常
- [ ] 可以保存 API Key
- [ ] 在弹窗中输入文本可以翻译
- [ ] 选中网页文本，点击插件图标，文本自动填充到弹窗
- [ ] 选中网页文本，出现翻译按钮
- [ ] 点击翻译按钮，显示翻译结果
- [ ] 右键菜单有翻译选项
- [ ] 右键菜单翻译功能正常
- [ ] 支持多种语言（中英日韩法德等）
- [ ] 错误提示清晰（API Key 未设置、网络错误等）

## 性能测试

测试不同长度的文本：

- [ ] 短文本（10 字符）
- [ ] 中等文本（100 字符）
- [ ] 长文本（1000 字符）
- [ ] 超长文本（5000 字符）

测试不同网站：

- [ ] 静态网页（HTML）
- [ ] 动态网站（React/Vue）
- [ ] 社交媒体（Twitter/Reddit）
- [ ] 技术文档（GitHub/MDN）

## 快速测试命令

在 `chrome-deepl-translator` 目录下运行：

```bash
# 打开测试页面
open test.html

# 查看文件结构
ls -la

# 查看图标是否生成
ls -la icons/

# 验证 manifest.json 格式
cat manifest.json | python3 -m json.tool
```

## 发布前最后检查

1. ✅ 所有文件都已保存
2. ✅ 图标文件存在（icons/ 目录）
3. ✅ manifest.json 格式正确
4. ✅ 所有功能测试通过
5. ✅ 文档齐全（README.md, GUIDE.md）
6. ✅ 没有敏感信息（API Key）
7. ✅ 代码中没有 console.log（或仅用于调试）

## 打包插件（可选）

如果要分享给他人：

```bash
# 1. 压缩整个文件夹
cd /Users/hogwarts/Documents/workspace/djangoRest
zip -r deepl-translator.zip chrome-deepl-translator -x "*.git*" -x "*__pycache__*" -x "*.pyc"

# 2. 或者在 Chrome 中打包
# chrome://extensions/ → 打包扩展程序 → 选择目录
```

---

**祝测试顺利！🎉**

有问题随时查看这个文档。
